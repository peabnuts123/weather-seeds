# To deploy in docker, copy this file and rename it to '.env.docker' and fill out values

# Postgres configuration (will be used to configure database on first run)
POSTGRES_USER=weather
POSTGRES_PASSWORD=
POSTGRES_DB=weather

# Environment configuration
NODE_ENV=production
