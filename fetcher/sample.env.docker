# To deploy in docker, copy this file and rename it to '.env.docker' and fill out values
# To debug locally, copy this file and rename it to '.env.local' and fill out values.
#   See README.md for info on debugging locally.

# Postgres configuration
POSTGRES_USER=weather
POSTGRES_PASSWORD=
POSTGRES_DB=weather

# Weather configuration
OPENWEATHERMAP_API_KEY=
DB_HOST=db

# Environment configuration
NODE_ENV=production
